// NOTE: If deploying to HTTPS (e.g. Netlify), ensure the server supports HTTPS
// or change these URLs to use HTTPS with a reverse proxy (nginx/cloudflare).
window.API_CONFIG = {
  userApi: 'https://server.344977.xyz:9000',
  chatApi: 'https://server.344977.xyz:9001',
};
